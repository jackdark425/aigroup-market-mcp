# ========= aigroup-market-mcp 环境变量模板 =========
# 复制本文件为 .env ，填入真实值再启动
# （注：以 # 开头的行会被 dotenv 忽略，可放心写注释）

# === 必填 ===
TUSHARE_TOKEN=YOUR_REAL_TUSHARE_TOKEN_HERE   # 去 https://tushare.pro/ 注册后获取

# === API配置（可选）===
# Binance API配置
# BINANCE_API_URL=https://api.binance.com    # Binance API地址，默认: https://api.binance.com
# BINANCE_TIMEOUT=10000                      # Binance请求超时时间（毫秒），默认: 10000

# Tushare API配置
# TUSHARE_TIMEOUT=30000                      # Tushare请求超时时间（毫秒），默认: 30000
# TUSHARE_RETRY_ATTEMPTS=3                   # Tushare请求重试次数，默认: 3

# === 分页配置（可选）===
# DEFAULT_PAGE_SIZE=1000                     # 默认分页大小，默认: 1000
# MAX_PAGE_SIZE=5000                         # 最大分页大小，默认: 5000

# === 导出配置（可选）===
# DEFAULT_EXPORT_PATH=./exports              # 默认导出目录，默认: ./exports
# CSV_EXPORT_PATH=./csv_exports              # CSV导出目录，默认: ./csv_exports
# JSON_EXPORT_PATH=./exports                 # JSON导出目录，默认: ./exports

# === 其他配置（可选）===
# NODE_ENV=development                       # dev / production，默认 dev

# ===========================================
